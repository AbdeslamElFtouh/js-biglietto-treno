Creazione delle variabili;
utilizzo di "prompt" per richiedere all'utente i dati;
imposizione delle condizioni per cui applicare gli sconti;
usare la funzione ".tofixed" per dare l'output con 2 decimali (centesimi di euro i questo caso);
inserire i messaggi di alert con gli output che l'utente avrà come risposta;
